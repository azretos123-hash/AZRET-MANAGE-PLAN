# YARIN V72 — Gold Saver Chart + Target Field Fix

- Gold chart X-axis now renders only evenly spaced labels that fit the available width.
- Fixed shared high-DPI canvas redraw bug that could make chart height grow/distort on repeated refreshes.
- First and last timestamps are preserved while dense intermediate labels are thinned.
- Gold target-price input now uses YARIN theme-aware premium form styling in Light and Dark mode.
- Mobile chart height and input readability hardened.
- Service-worker cache bumped to V72.
