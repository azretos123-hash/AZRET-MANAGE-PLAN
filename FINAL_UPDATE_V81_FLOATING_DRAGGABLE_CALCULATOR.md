# YARIN V81 — Floating Draggable Calculator

- Removed Calculator from the sidebar.
- Kept the complete Calculator page and all existing calculator features.
- Added a premium floating calculator shortcut.
- Tap/click opens Calculator; drag moves the shortcut anywhere inside the viewport.
- Position is persisted locally and restored after reload.
- Pointer/touch/keyboard support included.
- Mobile default position clears the bottom navigation and safe-area inset.
- Viewport resize clamps a saved position back on-screen.
- Service-worker cache bumped to v81 and main asset query strings bumped to v81.
